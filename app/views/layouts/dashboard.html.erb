<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <title>Signal - Alerting & Incidents</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <!-- Tailwind CSS via CDN (API-only app doesn't have asset pipeline) -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="h-full antialiased" style="font-family: 'Inter', system-ui, sans-serif; background: #FAF9F7;">
  <div class="min-h-full">
    <!-- Header -->
    <header class="border-b" style="border-color: #E8E5E0; background: #FFFFFE;">
      <div class="mx-auto max-w-5xl px-6">
        <div class="flex h-16 items-center justify-between">
          <%= link_to dashboard_root_path, class: "flex items-center gap-2.5" do %>
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M10 2L2 10L10 18L18 10L10 2Z" fill="#F97316"/>
              <circle cx="10" cy="10" r="3" fill="#FFFFFE"/>
            </svg>
            <span class="text-[15px] font-semibold tracking-tight" style="color: #1A1A1A;">Signal</span>
          <% end %>

          <% if @project&.persisted? %>
            <div class="flex items-center gap-6">
              <div class="flex items-center gap-3">
                <span style="color: #C4C0B8;">/</span>
                <%= link_to @project.name, dashboard_project_alerts_path(@project), class: "text-[14px] hover:underline", style: "color: #6B6760;" %>
              </div>
              <div class="flex items-center gap-4">
                <% alerts_active = controller_name == 'alerts' %>
                <% incidents_active = controller_name == 'incidents' %>
                <% rules_active = controller_name == 'rules' %>
                <% channels_active = controller_name == 'channels' %>
                <% escalation_active = controller_name == 'escalation_policies' %>
                <% schedules_active = controller_name == 'on_call_schedules' %>
                <% maintenance_active = controller_name == 'maintenance_windows' %>
                <% settings_active = action_name == 'edit' && controller_name == 'projects' %>

                <%= link_to dashboard_project_alerts_path(@project), class: "text-[13px] font-medium transition-colors", style: alerts_active ? "color: #1A1A1A;" : "color: #8B8780;" do %>
                  Alerts
                <% end %>
                <%= link_to dashboard_project_incidents_path(@project), class: "text-[13px] font-medium transition-colors", style: incidents_active ? "color: #1A1A1A;" : "color: #8B8780;" do %>
                  Incidents
                <% end %>
                <%= link_to dashboard_project_rules_path(@project), class: "text-[13px] font-medium transition-colors", style: rules_active ? "color: #1A1A1A;" : "color: #8B8780;" do %>
                  Rules
                <% end %>
                <%= link_to dashboard_project_channels_path(@project), class: "text-[13px] font-medium transition-colors", style: channels_active ? "color: #1A1A1A;" : "color: #8B8780;" do %>
                  Channels
                <% end %>
                <%= link_to dashboard_project_escalation_policies_path(@project), class: "text-[13px] font-medium transition-colors", style: escalation_active ? "color: #1A1A1A;" : "color: #8B8780;" do %>
                  Escalation
                <% end %>
                <%= link_to dashboard_project_on_call_schedules_path(@project), class: "text-[13px] font-medium transition-colors", style: schedules_active ? "color: #1A1A1A;" : "color: #8B8780;" do %>
                  On-Call
                <% end %>
                <%= link_to dashboard_project_maintenance_windows_path(@project), class: "text-[13px] font-medium transition-colors", style: maintenance_active ? "color: #1A1A1A;" : "color: #8B8780;" do %>
                  Maintenance
                <% end %>
                <%= link_to edit_dashboard_project_path(@project), class: "text-[13px] font-medium transition-colors", style: settings_active ? "color: #1A1A1A;" : "color: #8B8780;" do %>
                  Settings
                <% end %>
              </div>
              <%= link_to dashboard_projects_path, class: "text-[13px] font-medium px-3 py-1.5 rounded-lg transition-colors hover:opacity-80", style: "background: #F0EFED; color: #6B6760;" do %>
                Projects
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    </header>

    <!-- Main -->
    <main class="mx-auto max-w-5xl px-6 py-10">
      <% if flash[:notice] %>
        <div class="mb-6 px-4 py-3 rounded-lg text-[14px]" style="background: #E8F5E8; color: #2D5A2D;">
          <%= flash[:notice] %>
        </div>
      <% end %>
      <% if flash[:alert] %>
        <div class="mb-6 px-4 py-3 rounded-lg text-[14px]" style="background: #FEE8E8; color: #8B2D2D;">
          <%= flash[:alert] %>
        </div>
      <% end %>
      <%= yield %>
    </main>
  </div>
</body>
</html>
